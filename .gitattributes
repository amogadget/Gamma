# Shell scripts must keep LF endings (executed inside Linux containers)
*.sh text eol=lf
# Batch files need CRLF (cmd.exe mis-parses labels/goto with LF-only)
*.bat text eol=crlf
# PDF fixtures contain byte offsets and fixed-width xref records.
*.pdf binary
