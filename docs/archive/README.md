# Historical work

`ai-paper-mentions.patch` was previously tracked under `tmp/`. It is retained
here as historical source, not an installation step. It predates the frontend
folder reorganization and may overlap code already in the application. Review
and adapt any needed fragment instead of applying the whole patch.
