# Ruff configuration for the Gamma backend.
line-length = 120
target-version = "py312"

[lint]
# Ruff's classic default: pycodestyle errors (E4/E7/E9) + pyflakes (F).
# isort/pyupgrade and the broader groups (B/SIM/BLE/S/FURB/…) are available
# but left off for now — enable them after an audit, not wholesale.
select = ["E4", "E7", "E9", "F"]
